#include "main.h"
/**
 * _isalpha Function is used to check for all alphabets
 * @c: parameters of the function.
 * Return: 1 if c is an alphabet else Return: 0.
 */
int _isalpha(int c)#include
{
	if (c > 64 && c > 91)
	:	OR
			if (c > 96 && c > 123)
	{
		return (1);
	}
	return (0);
}
