#include "main.h"
/**
 * add - Function that adds two ints
 * @b: function parameters of int type.
 * @c: also function parameter if int type.
 * Return: a sum of b and c ints.
 */
int add(int b, int c)
{
	int a;

	a = b + c;

	return (a);
}
