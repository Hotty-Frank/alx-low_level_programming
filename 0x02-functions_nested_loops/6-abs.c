#include "main.h"
/**
 *_abs - Function that prints absolute vales od integers
 *@i: Function parameters..
 *Return: absolute vaue of int ans appropriate type.
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	return (i);
}
